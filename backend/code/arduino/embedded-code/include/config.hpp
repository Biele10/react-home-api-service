#pragma once
#include <stdint.h>

namespace Config
{
    constexpr uint8_t RED_LED_PIN = 11;
    constexpr uint8_t BASE_ARRAY_SIZE = 10;
    constexpr uint8_t DEFAULT_ARRAY_ADDITION = 5;       // default amount to increase an array size by
}